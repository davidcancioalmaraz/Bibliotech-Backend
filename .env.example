DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=bibliotech-password
DB_DATABASE=postgres
DB_MIGRATIONS_RUN=false
DB_LOGGING=false

# Seeding (opcional)
SEED_BOOK_COUNT=25
SEED_FRESH=false
SEED_FAKER_SEED=

# Auth
JWT_SECRET=bibliotech-dev-secret-cambiar-en-produccion
JWT_EXPIRES_IN=1d

# CORS (opcional): lista separada por comas; vacío = cualquier origen
CORS_ORIGIN=
